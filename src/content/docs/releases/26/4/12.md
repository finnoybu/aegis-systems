---
title: "April 12, 2026"
description: "Development log for April 12, 2026"
template: doc
sidebar:
  hidden: true
---

## Development Log

- Pinned Node 22.x LTS and npm 10.x in `.nvmrc`, `package.json` engines, and `CLAUDE.md` to prevent lockfile drift between local dev and Cloudflare Pages builds (#76)
- Regenerated `package-lock.json` under npm 10 so Cloudflare Pages builds succeed (#75)
- Resolved the header version badge by reading `git describe --tags` at build time instead of a stale dashboard environment variable (#77)
- Fixed the nightly release rollup: date-drift bug, Dependabot skip, and enforced the 4-part dev tag / 3-part release tag scheme (#78)
- Refactored version resolution to read from a committed `VERSION` file at the repo root — one source of truth, works under shallow clones (#79)
- Backfilled the v26.4.11 release entries into the monthly rollup and index, pinned the VERSION file to v26.4.11, and merged in the April 12 dev log (#80)
- chore: remove duplicate legal pages, defer to canonical aegis-initiative source (4a56c0a)
- chore: drop orphaned local copies of design-system components (#85) (7562dbb)
